# Bootstrap-Portfolio

## HTML
Structured html utilising Bootstrap 4.
From Bootstrap 4, used Layout, Components and Unitilites.
Gave tags semantic structure.
Provided comments to make it easy to follow the html document.

### CSS
Targeted classes, IDs and tags to personalised the web page and overwite the Bootstrap's original features.
Structured CSS file to follow the same order as html, so it is easy to follow.
Provided comments in the CSS file, so it is easy to follow.
Checked to make sure that where possible I targeted all relevant sections in one go to follow the keep is DRY value. 

#### Images
Downloaded images from internet.
Saved them into the Images folder. 
Linked images in the relevant sections in both html and CSS files. 

